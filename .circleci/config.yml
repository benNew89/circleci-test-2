version: 2.1

jobs:
  hello-world:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: Say hello
          command: echo "Hello world"

workflows:
  say-hello:
    jobs:
      - hello-world
